# Yakushi Control Deck v1.2.15

Power-menu alignment hardening.

- Replaced font glyph power actions with bundled 64x64 SVG assets.
- Rofi renders every action through the same fixed-size `element-icon` box.
- Removes optical baseline drift between Lock, Suspend, Logout, Reboot and Shutdown.
- Fresh installs and upgrades deploy the icon assets under `~/.config/rofi/icons/power/`.
